# The same application as bench/bench_app.rb, in Soli: one component, the
# same tree, so the comparison is of the two servers and not of two views.
router_eui("bench", "bench#bench", "bench#bench_view")
